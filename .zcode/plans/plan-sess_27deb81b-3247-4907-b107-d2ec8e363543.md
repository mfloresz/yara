Implement the reviewed fix with focused regression coverage:

1. Correct partial chapter character-count updates in `internal/store/store_chapters.go`.
   - Keep the existing merge behavior for content fields.
   - When counts are recalculated, read the merged values from the PocketBase record (`original_content`, `translated_content`, `refined_content`) instead of using only the incoming patch fields. This preserves translated/refined counts during re-downloads and fixes all other partial chapter updates.
   - Leave unrelated chapter ordering behavior unchanged to keep this correction narrowly scoped.

2. Add regression assertions to the redownload API test (`internal/api/redownload_test.go`).
   - Extend its chapter response fixture with the persisted character-count fields.
   - Verify chapters whose translations/refinements are preserved also retain correct translated/refined character counts after the re-download job.
   - Verify novel-level chapter statistics remain correct after the job, so the test catches both per-chapter and aggregate corruption.

3. Run verification:
   - `go test ./internal/store ./internal/api`
   - `go test -short ./...` (report any unrelated external-provider failures).
   - `cd frontend && npm run build` to ensure the existing frontend changes still typecheck/build.

No schema or migration is needed because this changes only how existing persisted count fields are computed during updates.